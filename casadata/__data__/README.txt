This directory holds the casadata package data tables.  These tables are large and must be retrieved from Github
after the casadata package installation.

If there is nothing else in this directory, then you haven't done that yet.

try something like:

from casadata import pull_data
pull_data()